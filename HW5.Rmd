---
title: "Homework 5"
author: "Elizabeth Fritz"
date: "November 29, 2023"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
# packages needed
library("readr")
library("dplyr")
library("lubridate")
library("ggplot2")
library("scales")
library("ggthemes")
```


```{r}
# reading in homicides data
homicides <- read_csv("data/homicide-data.csv")

# creating city_state and date variables
baltimore_homicides <- homicides %>%
  mutate(city_state = paste(city, state, sep = ", ")) %>%
  filter(city_state == "Baltimore, MD") %>%
  mutate(reported_date = ymd(reported_date),
         reported_month = month(reported_date),
         reported_year = year(reported_date),
         reported_month_year = paste(reported_month, reported_year, sep = "-"), 
         season = case_when(reported_month %in% c(1:4, 11:12) ~ "Winter",
                            reported_month %in% c(5:10) ~ "Summer"))
         

```

```{r}

# group by month year to count monthly count of cases
# plot monthly count by reported_date???


test <- baltimore_homicides %>%
  group_by(reported_month, reported_year) %>%
  mutate(monthly_count =(n())) %>%
  ggplot() + 
  geom_bar(aes(x = reported_date, fill = season)) +
  theme_few() +
  theme_dark()

test
```


